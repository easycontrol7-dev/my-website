# my-website

YouTube Script Benchmark

## Deployment

This website is automatically deployed to GitHub Pages when changes are pushed to the `main` branch.

The deployment workflow is located at `.github/workflows/deploy.yml` and uses GitHub Actions to:
- Build and package the site
- Deploy to GitHub Pages

To enable GitHub Pages for this repository:
1. Go to Settings → Pages
2. Under "Source", select "GitHub Actions"
3. The site will be available at `https://easycontrol7-dev.github.io/my-website/`
